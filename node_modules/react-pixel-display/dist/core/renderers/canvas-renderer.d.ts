import { EffectManager } from '../effects';
import { RGBColor, PixelArray } from '../types';
export declare class CanvasLEDRenderer {
    width: number;
    height: number;
    private pixelGap;
    private glowEnabled;
    private scale;
    buffer: RGBColor[];
    private _colorPixels;
    private _extendedColorPixels;
    extendedWidth: number;
    private effect;
    private speed;
    private animationId;
    private lastFrameTime;
    private _isRunning;
    private _canvas;
    private _ctx;
    private _glowCanvas;
    private _glowCtx;
    private _wrapper;
    private _canvasCreated;
    container: HTMLElement | null;
    effectManager: EffectManager;
    constructor(container: HTMLElement | null, options?: {
        width?: number;
        height?: number;
        pixelGap?: number;
        glow?: boolean;
        scale?: number;
    });
    private _initBuffer;
    private _createCanvas;
    private _ensureCanvasInContainer;
    setPixel(x: number, y: number, color: RGBColor): void;
    clear(): void;
    flush(): void;
    private _roundRect;
    setData(pixels: PixelArray, extendedPixels?: PixelArray | null, extendedWidth?: number | null): void;
    setEffect(effect: string, speed?: number): void;
    start(): void;
    stop(): void;
    get isRunning(): boolean;
    private _animate;
    private _renderFrame;
    renderStatic(): void;
    setDimensions(width: number, height: number): void;
    setContainer(container: HTMLElement): void;
    destroy(): void;
}
