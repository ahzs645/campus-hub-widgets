import { RGBColor } from './types';
/**
 * Convert hex color string to RGB array.
 * Handles short-circuit cases for common dark/background colors.
 */
export declare function hexToRgb(hex: string): RGBColor;
/**
 * Convert hex color to RGB object (used by canvas font renderer).
 */
export declare function hexToRgbObj(hex: string): {
    r: number;
    g: number;
    b: number;
};
/**
 * Convert HSV values to RGB color.
 * @param h - Hue (0-1)
 * @param s - Saturation (0-1)
 * @param v - Value/brightness (0-1)
 */
export declare function hsvToRgb(h: number, s: number, v: number): RGBColor;
