import { PixelArray, HexColor, FontResolver, BdfFontConfig, ScrollPixelsResult } from '../types';
export declare const BDF_FONT_CONFIG: Record<string, Record<number, BdfFontConfig>>;
interface CachedFont {
    font: {
        draw: (text: string, options: {
            direction: string;
            mode: number;
        }) => {
            bindata: string[];
            width: () => number;
            height: () => number;
        };
    };
    config: BdfFontConfig;
}
/**
 * Set a custom font URL resolver for BDF fonts.
 */
export declare function setFontResolver(resolver: FontResolver): void;
/**
 * Get closest height key for font metrics (16, 24, or 32).
 */
export declare function getHeightKey(height: number): number;
/**
 * Load a BDF font for a specific size.
 */
export declare function loadBdfFont(fontName: string, heightKey?: number, resolver?: FontResolver): Promise<CachedFont | null>;
/**
 * Check if a BDF font is loaded.
 */
export declare function isBdfFontLoaded(fontName: string, heightKey?: number): boolean;
/**
 * Render text to pixels using BDF font.
 */
export declare function textToPixelsBdf(text: string, width: number, height: number, fgColor?: HexColor, bgColor?: HexColor, fontName?: string): PixelArray | null;
/**
 * Render text for scrolling (extended width for seamless loop).
 */
export declare function textToScrollPixelsBdf(text: string, displayWidth: number, height: number, fgColor?: HexColor, bgColor?: HexColor, fontName?: string): ScrollPixelsResult | null;
/**
 * Preload all BDF fonts for common heights.
 */
export declare function preloadBdfFonts(resolver?: FontResolver): Promise<void>;
/**
 * Get list of available BDF fonts.
 */
export declare function getAvailableBdfFonts(): string[];
export {};
