import { FontResolver } from '../types';
export { textToPixels, textToScrollPixels, pixelFont } from './legacy-font';
export { loadFont, isFontLoaded, textToPixelsCanvas, textToScrollPixelsCanvas, preloadFonts, FONT_METRICS, setFontResolver as setTtfFontResolver, getHeightKey, } from './canvas-font';
export { loadBdfFont, isBdfFontLoaded, textToPixelsBdf, textToScrollPixelsBdf, preloadBdfFonts, getAvailableBdfFonts, BDF_FONT_CONFIG, setFontResolver as setBdfFontResolver, getHeightKey as getBdfHeightKey, } from './bdf-font';
/**
 * Configure font URL resolution for both TTF and BDF fonts.
 */
export declare function configureFonts(options: {
    ttfResolver?: FontResolver;
    bdfResolver?: FontResolver;
    baseUrl?: string;
}): void;
