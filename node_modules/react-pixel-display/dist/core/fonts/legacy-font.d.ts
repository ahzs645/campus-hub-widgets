import { PixelArray, HexColor, ScrollPixelsResult } from '../types';
/**
 * 5x7 Pixel font for LED display rendering.
 * Each character is represented as 5 bytes (columns), with bits representing rows.
 */
export declare const pixelFont: Record<string, number[]>;
/**
 * Render text to a pixel array (centered, clipped to display width).
 */
export declare function textToPixels(text: string, width: number, height: number, fgColor?: HexColor, bgColor?: HexColor): PixelArray;
/**
 * Render text to an extended pixel array for scrolling.
 * Width is: displayWidth + textWidth + displayWidth (for seamless wrap).
 */
export declare function textToScrollPixels(text: string, displayWidth: number, height: number, fgColor?: HexColor, bgColor?: HexColor): ScrollPixelsResult;
