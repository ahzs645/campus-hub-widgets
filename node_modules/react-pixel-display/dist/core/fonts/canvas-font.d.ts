import { PixelArray, HexColor, FontResolver, FontMetrics, ScrollPixelsResult } from '../types';
export declare const FONT_METRICS: Record<string, Record<number, FontMetrics>>;
/**
 * Set a custom font URL resolver for TTF fonts.
 */
export declare function setFontResolver(resolver: FontResolver): void;
declare function getHeightKey(height: number): number;
/**
 * Load a TTF font as @font-face.
 */
export declare function loadFont(fontName: string, resolver?: FontResolver): Promise<boolean>;
/**
 * Check if a TTF font is loaded.
 */
export declare function isFontLoaded(fontName: string): boolean;
/**
 * Render text to pixels using Canvas (matches pypixelcolor output).
 */
export declare function textToPixelsCanvas(text: string, width: number, height: number, fgColor?: HexColor, bgColor?: HexColor, fontName?: string): PixelArray | null;
/**
 * Render text for scrolling (extended width for seamless loop).
 */
export declare function textToScrollPixelsCanvas(text: string, displayWidth: number, height: number, fgColor?: HexColor, bgColor?: HexColor, fontName?: string): ScrollPixelsResult | null;
/**
 * Preload all available TTF fonts.
 */
export declare function preloadFonts(resolver?: FontResolver): Promise<void>;
export { getHeightKey };
