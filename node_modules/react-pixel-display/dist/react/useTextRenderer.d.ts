import { FontType, FontResolver, PixelArray, HexColor, ScrollPixelsResult } from '../core/types';
interface UseTextRendererOptions {
    width: number;
    height: number;
    text: string;
    font?: FontType | string;
    fontName?: string;
    foregroundColor?: HexColor;
    backgroundColor?: HexColor;
    fontResolver?: FontResolver;
    needsScroll?: boolean;
}
export declare function useTextRenderer(options: UseTextRendererOptions): {
    pixels: PixelArray | null;
    scrollData: ScrollPixelsResult | null;
    fontReady: boolean;
};
export {};
